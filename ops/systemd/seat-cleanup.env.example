# Copy this template to:
# /etc/nusagiliboat/seat-cleanup.env
#
# The installed file must be owned by root with mode 0600.
# Never commit the actual CRON_SECRET.

CLEANUP_ENDPOINT_URL=https://approved-preview.example.com/api/internal/expire-held-bookings
CRON_SECRET=REPLACE_WITH_A_RANDOM_64_CHARACTER_SECRET
